# Copier vers .env dans ce même dossier si on lances Prisma depuis src/config/
# Le fichier pointe vers la base à la racine du repo (même emplacement que le .env racine avec file:./dev.db)
DATABASE_URL="file:../../dev.db"
