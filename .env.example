# Copier vers .env et adapter les valeurs.
# Pour PostgreSQL (comme dans schema.prisma), l’URL doit commencer par postgresql:// ou postgres://

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/NOM_DE_LA_BASE"
